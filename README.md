# Flutter Voyage🚀

Thank you for joining the ship!

Let's dive into what we'll be doing in this course.

## Table of Content

- Introduction
- Dart Basics:
  - Expressions & variables
  - Types & Operators,
  - Control Flows
  - Functions & Classes
  - Collections
- Creating your first Flutter app: A Student app
- Basic widgets
- Stateless vs stateful widget

## Exercises

- **Expresions & Variables**

1. what is dart `entry point`?
2. Print `"Hello, Voyagers"` on the console.
3. Assign your name to a variable, and print it on the console.
4. Write this on dart code: `x is 10`, and `x is 50`.

- **Types & Operators**

5. What’s the type of value?

   `var value = 10 / 2;`

- **Control Flows**

6. Boolean challenge:
   In each of the following statements, what is the value of the Boolean expression?
   ```dart
   true && true
   false || false
   (true && 1 != 2) || (4 > 3 && 100 < 1)
   ((10 / 2) > 3) && ((10 % 2) == 0)
   ```

- **Functions**

7. Find the peremeter of a recteangle using this formular: `P=2(l+w)`, return the result whenever l and w is passed as a peremeter/argument.

- **Classes**

8. Create a `class` called `Car` with 4 properties you can think of a `Car`. Then, create 3 different instances of that `Car` object.
